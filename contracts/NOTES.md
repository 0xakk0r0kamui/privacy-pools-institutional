# integration notes

- currently, the pool only integrates with
  - `AssociationSetRegistry` (set id -> root reference)
  - `IComplianceVerifier` (external proof verification)
- next, this can be connected to core privacy pool logic (entrypoint/impls)
  once policy + proof formats are finalized

